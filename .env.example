# Copia in .env.local per lo sviluppo locale (non committare segreti).

# Opzionale: in build produzione, logga Core Web Vitals in console (utile con Remote debugging su mobile).
# Richiede rebuild + deploy per avere effetto su https://gpx.salentobike.com
# NEXT_PUBLIC_WEB_VITALS_LOG=1
